// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a[]={1,2,3,4,5};
   int s=0;
   int size=sizeof(a)/sizeof(a[0]);
   int e=size-1;
   while(s<e)
   {
       int temp=a[s];
       a[s]=a[e];
       a[e]=temp;s++;e--;
   }
   for(int i=0;i<size;i++)
   {
       printf("%d",a[i]);
   }
   
    return 0;
}
54321
